# HireWise Ai Local

## Run locally with yarn

```bash
yarn install
yarn dev
```

Then open:

```text
http://localhost:3000
```

## Where data is stored

All local data is saved next to the site in:

```text
local-data/db.json
local-data/uploads/
```

## Production mode

```bash
yarn install
yarn build
yarn start
```
